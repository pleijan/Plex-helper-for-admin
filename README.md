# add_film_on_plex

# Prerequisites

you just need to have docker and docker compose

# How to run the project

`
docker compose up -d --build 
`

# Access the project

you just need to connect to the IP address of the host machine
